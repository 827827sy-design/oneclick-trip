server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/oneclick_trip?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: your-mysql-password

app:
  jwt:
    secret: replace-with-a-long-random-dev-secret
    expiration-minutes: 1440
